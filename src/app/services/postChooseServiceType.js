const postChooseServiceType = async (req, res) => {
  // Validate Input
  // Write input to sesssion
  // Redirect to /users page (for now, until next page is done)
  return res.redirect("/users");
};

module.exports = postChooseServiceType;
