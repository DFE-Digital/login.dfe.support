{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Support",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "env": {
                "settings": "${workspaceFolder}/config/local-config.json"
            },
            "program": "${workspaceFolder}/src/index.js",
            "outputCapture": "std"
        }
    ]
}
